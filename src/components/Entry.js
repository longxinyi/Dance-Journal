import React from "react";

const Entry = (props) => {
  return (
    <React.Fragment>
      <header>Dance video</header>
      <h2>date</h2>
      <input></input>
    </React.Fragment>
  );
};

export default Entry;
